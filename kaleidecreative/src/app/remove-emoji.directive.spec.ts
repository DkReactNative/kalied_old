import { RemoveEmojiDirective } from './remove-emoji.directive';

describe('RemoveEmojiDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveEmojiDirective();
    expect(directive).toBeTruthy();
  });
});
